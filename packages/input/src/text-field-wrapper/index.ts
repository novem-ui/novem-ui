import styled from '@emotion/styled'
import { spacing, SpacingProps } from '@novem-ui/base'

const TextFieldWrapper = styled.div<SpacingProps>`
  position: relative;
  display: flex;
  flex-direction: column;
  ${spacing}
`

export default TextFieldWrapper
